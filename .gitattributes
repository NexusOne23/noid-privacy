# Normalize all text files to LF in the repository and on checkout.
# The engine is authored and released with LF endings on every platform.
* text=auto eol=lf

# Never touch binary content.
*.png binary
*.ico binary
